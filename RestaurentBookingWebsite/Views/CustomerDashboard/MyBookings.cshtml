@model IEnumerable<RestaurentBookingWebsite.DbModels.Booking>

@{
    ViewData["Title"] = "MyBookings";
}

<h1>MyBookings</h1>


<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BookingDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SlotTime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Status)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CreationTime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BookingId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BookingDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SlotTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Status)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreationTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BookingId)
            </td>
            <td>
                @* <a asp-action="Edit" asp-route-id="@item.BookingId">Edit</sss> |
                <a asp-action="Details" asp-route-id="@item.BookingId">Details</a> | *@
                <a asp-action="CancelBooking" method="post" asp-route-id="@item.BookingId">Cancel</a>
                    @* @Html.ActionLink("Cancel Booking", "CancelBooking", new { id = item.BookingId }) *@
                
                @* @Html.ActionLink("Cancel", "CancelBooking", new { id = item.BookingId })  *@
            </td>
        </tr>
}
    </tbody>
</table>
